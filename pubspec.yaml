name: number_validation_app
description: A new Flutter project.
publish_to: 'none'
version: 0.1.0

environment:
  sdk: '>=3.0.0-290.0.dev <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  modal_bottom_sheet: ^2.1.2
  country_codes: ^2.2.0
  dash_flags: ^0.0.4
  blurrycontainer: ^1.0.2
  mask_text_input_formatter: ^2.4.0
  provider: ^6.0.5
  

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.0

flutter:
  uses-material-design: true
